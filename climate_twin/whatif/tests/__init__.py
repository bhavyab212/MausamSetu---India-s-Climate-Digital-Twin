"""
whatif.tests — pytest suite for the scenario engine.

# TODO(Part 1+): unit tests per layer; end-to-end scenario reproducibility
#               test (same seed + same manifest sig → byte-equal hash).
"""
